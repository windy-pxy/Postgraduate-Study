# AI 与程序施工规则

本项目是个人“数学一 + 408”学习工具。当前目标是直接阅读原始资料，用 AI 解释知识点、分析作答，并按需保存自己的理解、错题和复盘。停止主动扩建整书 Markdown 转换、OCR、增强解析、审核流水线、自动评分、批量导入和知识图谱；旧功能与旧产物保留，只有用户提出具体问题时才维护。

1. 项目边界为 `D:\Postgraduate-Study`，不得访问边界外的用户目录，不修改系统环境，不新增模型服务、插件或 API。
2. `sources-original/` 和 `import-inbox/` 中的资料永久保留。已有原件不得修改、覆盖、重命名、移动或删除；来源 manifest 与真实 SHA-256 基线不得用重建方式掩盖异常。
3. 阅读教材以原始 PDF 和对应页面图片为准。解析 Markdown 只是辅助线索；题干、公式或符号冲突时查看原页，不得猜补。
4. 保留教材完整页面图片、题目截图、局部插图以及它们和 source_id、PDF 物理页码的映射。图片序号不能擅自解释为页码；映射不明时标注待确认。
5. 日常学习入口固定为：数学一、408、错题本、周复盘、原始资料。技术指南与旧工具放在“系统维护”，不放到学习主流程。
6. Claudian 日常辅导可以使用一般知识，并明确标为“模型补充”；引用用户资料时必须给真实来源和页码。只有用户明确要求“严格资料查询”时才使用 accepted-only 规则。
7. 只有用户要求“保存笔记”“加入错题本”或“更新笔记”时才写入。只读模式下输出可复制 Markdown。缺失的作答、错误原因、来源和学习记录留空，不得编造。
8. 日常只推荐 `知识笔记模板.md` 和 `错题模板.md`。旧模板保留在系统维护范围，不批量创建空白笔记；双链只连接确实存在且相关的笔记。
9. 所有删除必须有明确授权。获准清理的未跟踪运行产物优先移入项目内、被 Git 忽略的 `.cleanup-staging/`；不得借清理删除原件、图片、manifest、人工校正、个人笔记或错题。
10. API Key 不得写入代码、Markdown、配置样例、Git、终端输出或日志。`.env` 不得读取或提交。
11. 路径操作必须拒绝符号链接、目录联接、路径穿越和项目外路径。不得用解析产物覆盖原件。
12. 修改前后检查 Git。只暂存当前任务文件；原件、inbox、真实 manifest、真实基线、解析产物、图片、模型、缓存、日志、索引和 `.obsidian` 均不得提交。未经明确授权不 push、不重写历史。
13. 验证应与本次改动相称。首页、链接、模板和 Claudian 指令只做必要检查；不为文案反复跑全量测试，也不把静态检查描述为真实模型问答成功。

Windows ACL 未启用。SHA-256 用于内容一致性校验，不是数字签名。
