# AI 与程序施工规则

本规则适用于整个项目及后续阶段。项目边界为 `D:\Postgraduate-Study`，不得访问边界外的用户目录，不得安装软件或修改系统环境。

1. `sources-original/` 中原始资料永久只读，任何程序和 AI 不得修改、覆盖、重命名、移动或删除。Phase 0 仅创建空目录及 `.gitkeep`；以后新增原件须由用户明确安排。
2. `vault/90-Parsed-Sources/` 是后续生成的解析产物，不是原件。不得用解析产物覆盖原件。
3. `vault/` 内的个人笔记允许按任务修改；修改前检查 Git 状态，保留用户尚未提交的工作。
4. 不覆盖同名文件。新建时先检查目标是否存在，存在则停止；已有文件的编辑必须属于用户明确授权范围。
5. 所有删除操作必须获得用户明确确认；普通“删除”默认设计为移动到 `archive/`，仍须确认。不得永久删除现有文件；原始资料不适用归档删除。
6. 未经用户确认不得批量导入、转换或执行其他批量资料操作。阶段内明确授权的骨架创建与测试可以执行。
7. API Key 仅通过运行环境变量提供。不得写入代码、Markdown、配置样例或 Git，不得在日志、异常、测试输出中输出 API Key；不得读取或输出真实 `.env` 内容进行检查。
8. 每个阶段单独实施、测试和汇报，完成后等待确认。当前仅 Phase 0，不实现 PDF 解析、AI 问答或联网模型调用。
9. 数学公式统一使用 LaTeX：行内 `$...$`，独立公式 `$$...$$`。
10. 后续知识问答必须标注资料来源和页码；无法确定页码或资料内容时明确说明，不得编造内容、引用或页码。
11. 原始 PDF、PPT、Word 等资料不得提交。修改前和完成后检查 Git 状态；未经用户明确确认不得执行 `git commit`。
12. 后续工具须先验证路径在项目内，并拒绝通过符号链接或目录联接越界；写入操作须额外拒绝原始资料目录。只读约束不能仅依赖 `.gitignore`。

这些是项目行为约束，不代表已经设置 Windows ACL。Phase 0 不修改系统权限；后续写入工具必须实现并测试路径保护。
