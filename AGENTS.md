# AI 与程序施工规则

本规则适用于整个项目及后续阶段。项目边界为 `D:\Postgraduate-Study`，不得访问边界外的用户目录，不得安装软件或修改系统环境。

1. `sources-original/` 中已有原始资料永久只读，任何程序和 AI 不得修改、覆盖、重命名、移动或删除。新增原件仅允许用户明确授权后，通过 Phase 2A 单文件计划及显式 `apply --apply` 首次复制；默认保留 inbox 文件。当前施工只建设基础设施，不导入真实资料。
2. `vault/90-Parsed-Sources/` 是后续生成的解析产物，不是原件。不得用解析产物覆盖原件。
3. `vault/` 内的个人笔记允许按任务修改；修改前检查 Git 状态，保留用户尚未提交的工作。
4. 不覆盖同名文件。新建时先检查目标是否存在，存在则停止；已有文件的编辑必须属于用户明确授权范围。
5. 所有用户文件删除操作必须获得用户明确确认；普通“删除”默认设计为移动到 `archive/`，仍须确认。不得永久删除现有文件；原始资料不适用归档删除。Phase 2A 已授权的例外仅限清理本次操作创建且身份相符的临时文件/空目录，以及测试程序在 `tests/.runtime/` 生成的独占合成沙箱；不得扩大为清理已有文件或正式落盘的原件。
6. 未经用户确认不得批量导入、转换或执行其他批量资料操作。阶段内明确授权的骨架创建与测试可以执行。
7. API Key 仅通过运行环境变量提供。不得写入代码、Markdown、配置样例或 Git，不得在日志、异常、测试输出中输出 API Key；不得读取或输出真实 `.env` 内容进行检查。
8. 每个阶段单独实施、测试和汇报，完成后等待确认。Phase 0、Phase 1、Phase 2A 已验收并提交；Phase 2B 仅包含数字原生 PDF 的本地基础文本提取、派生 Markdown、质量报告与合成测试。不调用网络/API，不安装 OCR、插件、Claudian 或大型模型，不解析 PPTX/DOCX，不导入真实资料。未经确认不自动进入后续阶段。
9. 数学公式统一使用 LaTeX：行内 `$...$`，独立公式 `$$...$$`。
10. 后续知识问答必须标注资料来源和页码；无法确定页码或资料内容时明确说明，不得编造内容、引用或页码。
11. 原始 PDF、PPT、Word 等资料不得提交。修改前和完成后检查 Git 状态；未经用户明确确认不得执行 `git commit`。
12. 后续工具须先验证路径在项目内，并拒绝通过符号链接或目录联接越界；写入操作须额外拒绝原始资料目录。只读约束不能仅依赖 `.gitignore`。
13. 正式笔记遵守 `vault/00-System/Metadata-Schema.md` 与 `Linking-Rules.md`，模板不得填入虚构数据。来源或关联不确定时进入 `vault/00-System/Review-Queue.md`，不得编造双链。
14. `scripts/validate_vault.py` 与健康检查通过 `source_manager.py` 只读检查完整性，不自动修复。`config/sources-original.baseline.json` 已升级为 SHA-256 身份锚点；完整 SHA-256 为内容一致性依据，文件名/大小/时间不能替代它。清单与原件冲突时必须停止，禁止通过删改或重建基线掩盖异常。
15. `scan`、`verify`、`list`、`status` 只读；`plan` 默认预览，仅明确 `--save` 写入 `review-queue/import-plans/`。`apply` 必须指定计划且默认 dry-run，仅明确 `--apply` 复制；不得模糊批量导入。分类必须由用户确认，不猜测；仅支持 PDF、PPTX、DOCX 并校验签名/结构。
16. 每个来源使用稳定 source_id、完整 SHA-256 及独立 JSON 清单；相同哈希不得重复导入，同名不同内容不得覆盖。原件与清单分别经临时文件、校验与不覆盖的原子发布；部分成功时保留原件并报错，不删除原件回滚。
17. 基线更新只能使用单独的 `baseline-update --apply`，且必须通过现有原件、清单和既有锚点验证。新来源在显式更新前显示 baseline_pending；默认命令不得改基线。清单/计划不得写入文件正文、密钥或绝对用户目录。
18. 不设置 Windows ACL，不修改 Obsidian 配置；Windows 只读属性不是强安全边界。SHA-256 不是数字签名，不能防止有写权限的人同时改动文件与校验记录；需保留可信历史和独立备份。日志不得包含正文、密钥或用户绝对路径。
19. `scripts/pdf_parser.py` 只能按已登记 source_id 读取完整性通过的 PDF。`parse` 默认 dry-run，只有明确 `--apply` 才能在 `vault/90-Parsed-Sources/<source_id>/` 新建派生产物；目标存在时拒绝覆盖。解析前后均核对原件哈希，原件始终只读。
20. PDF 输出必须标记 `derived: true` 与 `review_required`，逐页保留原始页码，失败页不得跳过。疑似公式、图片或空文本页只生成复核标记和必要页面预览，不得编造 LaTeX、标题结构或学科结论。正式知识笔记与解析产物永久分区。
21. Phase 2B 临时输出只能创建在 `vault/90-Parsed-Sources/` 下，失败时仅可清理本次创建且身份一致的 `.tmp-...` 目录；不得删除既有解析目录。仅在原件 SHA-256 通过、临时产物验证通过且目录原子发布后，才可用同目录临时文件和原子替换将来源 manifest 标记为 `parsed`;原件身份、哈希、分类和导入信息不得改写。发布与 manifest 不能合并为一个文件系统事务；中断留下的状态不一致必须报错并人工处理，不自动删除或重解析。
22. 解析正文必须忠实保留，不得自动删除、替换或脱敏后发布。高置信疑似真实密钥必须阻止整份产物发布，只记录固定错误码、页码和风险类型，不回显原文；`YOUR_API_KEY` 等占位符与普通代码示例不得被改写。

这些是项目行为约束，不代表已经设置 Windows ACL。Phase 0 不修改系统权限；后续写入工具必须实现并测试路径保护。
